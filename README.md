# CLI for the Jedox Logs API

Required:
* requests
* colorama

Use pip to install the packages

Usage:
python .\tail_api.py --cloud_instance name-of-your-instance --token your-token --level warning --service "In-Memory"


python .\tail_api.py --help for more options
